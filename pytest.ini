[pytest]
# src/ must be importable from tests/ so that the Phase 2/3 leakage tests
# activate the moment their modules land, instead of silently importorskip-ing
# forever.
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
